<?xml version="1.0" encoding="utf-8"?>
<com.example.everyoneassist.Layout.PercentLinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical">

    <RadioGroup
        app:layout_marginTopPercent="3%w"
        android:id="@+id/rgShopp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/white"
        app:layout_heightPercent="12%w">

        <RadioButton
            android:id="@+id/rbCity"
            android:textColor="@color/shopping_rb_bg"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="20dp"
            android:button="@null"
            android:checked="true"
            android:gravity="center"
            android:text="同城快递"
            android:textSize="15sp" />

        <RadioButton
            android:id="@+id/rbShopp"
            android:textColor="@color/shopping_rb_bg"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginLeft="20dp"
            android:button="@null"
            android:text="随意购"
            android:textSize="15sp" />
    </RadioGroup>

    <FrameLayout
        android:id="@+id/fl_contain"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</com.example.everyoneassist.Layout.PercentLinearLayout>